@using AppPerfumaria.Services

@inject UsuarioService UsuarioService

@if (UsuarioService.CheckPermissoes(Chaves))
{
    @Content
}

@code {
    [Parameter] public List<string>? Chaves { get; set; }
    [Parameter] public RenderFragment Content { get; set; }
}
